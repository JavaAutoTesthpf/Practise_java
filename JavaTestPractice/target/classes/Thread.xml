<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE suite SYSTEM "http://testng.org/testng-1.0.dtd" >

<suite name = "thread" parallel = "classes" thread-count = "3">
    <!--
    classes级别:相同的class tag下的用例在同一个线程下执行,不同的class下的用例在可以在不同线程下执行
    tests级别：不同的test tag下的用例可以在不同的线程中执行，相同的test tag下用例只能在同一线程下执行
    methods级别：所有用例都可以在不同线程下执行
    thread-count：代表了最大并发线程数
    xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池
    -->
    <test name = "demo1">
        <parameter name = "name" value = "张三"/>
        <parameter name = "age" value = "10"/>
        <classes>
            <class name ="com.practice.code.multiThread.MultiThreadOnXml"/>
            <class name ="com.practice.code.BasicAnnotation"/>
            <class name ="com.practice.code.paramter.ParamterTest"/>
        </classes>
    </test>

</suite>